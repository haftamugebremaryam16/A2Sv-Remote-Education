n,k=map(int,input().split())
arr=list(map(int,input().split()))
arr.sort()
if k>0:
    ans=arr[k-1]
else:
    ans=arr[0]-1
cnt=0
for i in arr:
    if i<=ans:
        cnt+=1
if cnt!=k or ans==0:
    print(-1)
else:
    print(ans)
