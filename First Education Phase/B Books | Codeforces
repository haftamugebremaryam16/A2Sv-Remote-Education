n,k=map(int,input().split())
arr=list(map(int,input().split()))
l=0
res=0
sum_=0
for r in range(n):
    sum_+=arr[r]
    while sum_>k:
        sum_-=arr[l]
        l+=1
    res=max(res,r-l+1)
print(res)
