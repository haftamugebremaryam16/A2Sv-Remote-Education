n,k=map(int,input().split())
arr=list(map(int,input().split()))
l=0
x=0
mx=float('-inf')
for r in range(len(arr)):
    x+=arr[r]
    while x>k:
        x-=arr[l]
        l+=1
    mx=max(mx,r-l+1)
print(mx)

